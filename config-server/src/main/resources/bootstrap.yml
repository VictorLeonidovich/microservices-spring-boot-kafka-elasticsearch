spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://${user.dir}/config-server-repository/
          clone-on-start: true
    fail-fast: true